// for now this file is useless but the folder is for document calls
// so the app can have a few different scripts going on and keeping them from making
// one giant script file.
// thought it would be a good idea, since its a desktop app afterall.