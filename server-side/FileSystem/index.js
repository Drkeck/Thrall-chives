// for now this will be empty but it will be the connection point
// between which functions get called and have them all reletively isolated from each other
// that's if its not really needed for them to interact at all.